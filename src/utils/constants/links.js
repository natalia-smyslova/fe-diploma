const domainName = '/fe-48-diplom';

const links = {
   main: domainName ,
   trains: `${domainName}/trains`,
   seats: `${domainName}/seats`,
   passengers: `${domainName}/passengers`,
   payment: `${domainName}/payment`,
   orderConfirmation: `${domainName}/orderConfirmation`,
   success: `${domainName}/success`,
};

export default links;